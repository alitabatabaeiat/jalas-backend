version: "3"
services:
  nginx:
    links:
      - node:node
    container_name: jalas-nginx
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - node

  node:
    container_name: jalas-node
    build:
      context: ../
      dockerfile: docker/node/Dockerfile